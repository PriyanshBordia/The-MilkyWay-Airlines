language: python

python:
    - "3.8"
env:
    - SECRET_KEY="0xbu216l&4@4!p&)1g(goq&l8deidtexxh2a$=*s1zm^&c4r8a"

deploy:
    - DATABASE_URL=$DATABASE_URL
    - DB_HOST=$DB_HOST
    - DB_NAME=$DB_NAME
    - DB_PASSWORD=$DB_PASSWORD
    - DB_PORT=$DB_PORT
    - DB_USER=$DB_USER

install:
    - pip3 install -r requirements.txt

script:
    - python3 manage.py makemigrations
    - python3 manage.py migrate

notifications:
  email:
    on_success: never # default: change
    on_failure: always
